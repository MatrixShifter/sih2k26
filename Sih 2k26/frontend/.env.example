# ==============================================================================
# ComplyGeM AI — Frontend Environment Variables (.env.example)
# ==============================================================================
# Used by: Vercel (Production) / Local Vite Development
# For local dev: Copy this file to .env.local (empty default proxies to localhost:8000).
# For Vercel: Add VITE_API_URL in Vercel Project Settings > Environment Variables.
# ==============================================================================

# Backend API Base URL on Render (no trailing slash)
VITE_API_URL=https://sih2k26-s3f0.onrender.com